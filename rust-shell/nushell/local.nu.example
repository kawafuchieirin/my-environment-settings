# =============================================================================
# local.nu - ローカル専用設定（例）
# =============================================================================
#
# このファイルを local.nu にコピーして使用してください。
# local.nu は .gitignore に含まれているため、コミットされません。
#
# 使用方法:
#   cp local.nu.example local.nu
#   hx local.nu
#
# =============================================================================

# -----------------------------------------------------------------------------
# 会社用の環境変数（例）
# -----------------------------------------------------------------------------

# APIキー
# $env.COMPANY_API_KEY = "your-api-key-here"
# $env.AWS_ACCESS_KEY_ID = "your-aws-key"
# $env.AWS_SECRET_ACCESS_KEY = "your-aws-secret"

# データベース接続
# $env.DATABASE_URL = "postgres://user:password@host:5432/dbname"

# 内部サービス
# $env.INTERNAL_SERVICE_URL = "https://internal.company.com"
# $env.SLACK_WEBHOOK_URL = "https://hooks.slack.com/services/xxx"

# -----------------------------------------------------------------------------
# 会社用のエイリアス（例）
# -----------------------------------------------------------------------------

# 社内ツール
# alias vpn = sudo openconnect vpn.company.com
# alias jira = open https://company.atlassian.net
# alias confluence = open https://company.atlassian.net/wiki

# 社内サーバー接続
# alias ssh-dev = ssh user@dev.company.com
# alias ssh-prod = ssh user@prod.company.com

# -----------------------------------------------------------------------------
# 会社用のPATH（例）
# -----------------------------------------------------------------------------

# $env.PATH = ($env.PATH | prepend $"($env.HOME)/company-tools/bin")

# -----------------------------------------------------------------------------
# 個人用の設定（例）
# -----------------------------------------------------------------------------

# $env.GITHUB_TOKEN = "ghp_xxxxxxxxxxxx"
# $env.OPENAI_API_KEY = "sk-xxxxxxxxxxxx"
